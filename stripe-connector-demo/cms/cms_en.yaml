Dialogs:
  com:
    axonivy:
      connector:
        stripe:
          demo:
            CreateCheckoutSessionDemo:
              createCheckoutSessionTitle: Create payment checkout session
              embedStripeCheckoutSession: Embed Stripe checkout Session form
            PaymentLinkDemo:
              createPaymentLink: Create Payment Link
              paymentLinkTitle: Create Payment Link Demo
